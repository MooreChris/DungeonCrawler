// this class handles some text constants
// import controlsInfo from './constants';

/*
this.add
    .text(550, 16, "Arrow keys to scroll", {
        font: "18px monospace",
        fill: "#ffffff",
        padding: { x: 20, y: 10 },
        backgroundColor: "#000000"
    })
    .setScrollFactor(0);

 */
const controlsInfo = {
    gameText: {
        '1': "Arrow keys to move",
        '2': "Space bar to Attack"
    }
}

export default controlsInfo;